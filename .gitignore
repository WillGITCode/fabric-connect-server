# ---- Generated server instances & downloads (never commit) ----
# Default install locations (setup installs to ~/Desktop, outside the repo,
# but ignore these in case someone installs INTO the repo folder):
/ModdedServer/
/ModdedServer-*/
/ModdedClient/
/FabricModdedServer/
/GateProxy/
*.jar
world/
world_nether/
world_the_end/
logs/
*.log

# ---- Secrets / tokens (never commit) ----
token.json
config.yml
FabricProxy-Lite.toml.local

# ---- macOS cruft ----
.DS_Store
